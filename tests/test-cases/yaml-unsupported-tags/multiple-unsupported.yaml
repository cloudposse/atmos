# This file contains multiple unsupported YAML tags
vars:
  # Multiple unsupported tags should all be caught
  custom_value: !custom "some-value"
  unknown_func: !unknown "another-value"
  bad_tag: !bad "bad-value"
  
  # Valid tag for comparison
  env_var: !env HOME