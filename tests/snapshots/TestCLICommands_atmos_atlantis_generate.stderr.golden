# Incorrect Usage

**Error:** atmos atlantis generate requires a subcommand.

Valid subcommands are:

  • repo-config

## Usage

 atmos atlantis generate [sub-command] [flags]

💡 Run atmos atlantis generate --help for usage
