# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: invalid

terraform:
  vars: {}

helmfile:
  vars: {}

settings: {}

env: {}

components:
  terraform:
    vpc:
      vars: "I'm not correctly configured"
