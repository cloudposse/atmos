# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  region: us-east-1
  environment: ue1

components:
  terraform:
    vpc:
      metadata:
        component: infra/vpc
      vars:
        availability_zones:
          - us-east-1a
          - us-east-1b
          - us-east-1c
