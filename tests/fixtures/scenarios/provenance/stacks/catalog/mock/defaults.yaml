# Base defaults for mock component (depth 3 in import chain).
components:
  terraform:
    mock:
      metadata:
        component: mock
      vars:
        enabled: true
        name: mock
        tags:
          managed_by: atmos
