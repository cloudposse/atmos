# Base defaults for myapp component (depth 3 in import chain).
components:
  terraform:
    myapp:
      metadata:
        component: myapp
      vars:
        enabled: true
        name: myapp
        tags:
          managed_by: atmos
