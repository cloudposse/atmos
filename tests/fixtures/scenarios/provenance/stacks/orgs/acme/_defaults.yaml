# Organization defaults for acme (depth 2 in import chain).
import:
  - mixins/stage/dev

vars:
  tenant: acme
  namespace: acme

components:
  terraform:
    mock:
      vars:
        tags:
          tenant: acme
