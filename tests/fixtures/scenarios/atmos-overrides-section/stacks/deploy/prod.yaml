# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: prod

import:
  - teams/team1
  - teams/team2

overrides: {}

terraform:
  overrides:
    vars:
      test:
        a: a-prod
        b: b-prod
        c: c-prod
