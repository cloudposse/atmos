# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: test

import:
  - teams/team1

overrides:
  vars:
    test:
      a: a-test
      b: b-test

terraform:
  overrides:
    vars:
      test:
        a: a-test-2
