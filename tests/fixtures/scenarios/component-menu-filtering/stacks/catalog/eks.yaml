# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

# EKS catalog configuration

components:
  terraform:
    eks:
      metadata:
        component: eks
      vars:
        cluster_name: "eks-cluster"
        kubernetes_version: "1.28"
        node_count: 3
