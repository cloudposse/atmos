# This stack has no explicit name - should use name_template: prod-ue2.
import:
  - catalog/base

vars:
  environment: prod
  stage: ue2

components:
  terraform:
    vpc:
      vars:
        cidr: "10.1.0.0/16"
