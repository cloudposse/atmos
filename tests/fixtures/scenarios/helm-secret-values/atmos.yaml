base_path: "."

components:
  helm:
    base_path: "components/helm"

stacks:
  base_path: "stacks"
  included_paths:
    - "deploy/**/*"
  name_template: "{{ .vars.stage }}"

stores:
  gcp-secrets:
    kind: gcp/secretmanager
    secret: true
    options:
      project_id: example-project
