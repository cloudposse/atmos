# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: reported-order-alias

components:
  terraform:
    platform-kms:
      metadata:
        component: mock

    platform-efs:
      metadata:
        component: mock

    uds-rds:
      metadata:
        component: mock
      dependencies:
        components:
          - name: platform-kms

    cloudposse-irsa-dbeaver-external-secrets:
      metadata:
        component: mock
      dependencies:
        components:
          - name: platform-kms
          - name: uds-rds

    cloudposse-irsa-external-secrets:
      metadata:
        component: mock
      dependencies:
        components:
          - name: platform-kms
          - name: uds-rds

    platform-cluster-codepipeline-app:
      metadata:
        component: mock
      dependencies:
        components:
          - name: platform-efs
          - name: platform-kms
          - name: uds-rds

    platform-datasync:
      metadata:
        component: mock
      dependencies:
        components:
          - name: platform-efs

    platform-uds-pkg-dbeaver:
      metadata:
        component: mock
      dependencies:
        components:
          - name: cloudposse-irsa-dbeaver-external-secrets
          - name: uds-rds
