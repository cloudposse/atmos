# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: nonprod

components:
  terraform:
    cmp-1:
      metadata:
        component: mock
      vars:
        foo: cmp-1-a
        bar: cmp-1-b
        baz: cmp-1-c
