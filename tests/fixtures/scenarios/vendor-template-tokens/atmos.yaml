base_path: "./"

components:
  terraform:
    base_path: "components/terraform"

stacks:
  base_path: "stacks"

logs:
  level: Debug

settings:
  # Enable token injection for all providers
  inject_github_token: true
  inject_gitlab_token: true
  inject_bitbucket_token: true
