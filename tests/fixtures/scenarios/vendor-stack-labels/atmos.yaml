base_path: "./"

components:
  terraform:
    base_path: "components/terraform"

stacks:
  base_path: "stacks"
  included_paths:
    - "**/*.yaml"
  name_template: "{{.vars.stage}}"

logs:
  file: "/dev/stderr"
  level: Info
