components:
  terraform:
    base_path: "components/terraform"

stacks:
  base_path: "stacks"
  name_template: "{{.vars.tenant}}-{{.vars.environment}}-{{.vars.stage}}"

logs:
  level: Info
  file: "/dev/stderr"

profiles:
  default: developer
