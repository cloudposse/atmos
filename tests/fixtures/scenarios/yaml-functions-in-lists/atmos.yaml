base_path: "."

components:
  terraform:
    base_path: "components/terraform"

stacks:
  base_path: "stacks"
  included_paths:
    - "deploy/**/*"
  excluded_paths:
    - "workflows/**/*"
  name_pattern: "{stage}"

schemas:
  jsonschema:
    base_path: "schemas/jsonschema"
  opa:
    base_path: "schemas/opa"
