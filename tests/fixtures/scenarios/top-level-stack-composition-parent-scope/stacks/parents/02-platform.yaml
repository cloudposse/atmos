vars:
  environment: dev
  stage: scoped
  owner: platform

components:
  terraform:
    chatops:
      metadata:
        component: mock
        inherits:
          - dns-primary
      vars:
        name: chatops
