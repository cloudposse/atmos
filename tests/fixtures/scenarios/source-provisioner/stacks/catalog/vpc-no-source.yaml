# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

# Component without source - for testing error handling
components:
  terraform:
    vpc-no-source:
      metadata:
        component: mock
      vars:
        enabled: true
        environment: "test"
