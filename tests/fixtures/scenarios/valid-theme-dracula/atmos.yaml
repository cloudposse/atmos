# Test configuration with valid Dracula theme
settings:
  terminal:
    theme: "dracula"

logs:
  level: Info
  file: /dev/stderr

stacks:
  base_path: "../../../stacks"

components:
  terraform:
    base_path: "../../../components/terraform"
    apply_auto_approve: false
    deploy_run_init: true
    init_run_reconfigure: true
    auto_generate_backend_file: false