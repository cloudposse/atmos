# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

components:
  terraform:
    demo:
      metadata:
        type: "abstract"
        labels:
          component: "demo"
          managed-by: "atmos"
      vars:
        name: "demo"
        enabled: true
