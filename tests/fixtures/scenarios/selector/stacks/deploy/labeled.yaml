# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

metadata:
  labels:
    env: "prod"
    tier: "backend"

vars:
  stage: "labeled"
  test_change: "added"

components:
  terraform:
    demo2:
      metadata:
        labels:
          version: "v2"
      vars:
        foo: "demo2"
