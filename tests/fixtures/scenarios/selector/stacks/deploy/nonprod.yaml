# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

metadata:
  labels:
    env: "dev"
    tier: "backend"
    team: "platform"

vars:
  stage: "dev"

import:
  - catalog/shared

components:
  terraform:
    demo:
      # No component-level labels - inherit from stack
      vars:
        foo: "demo-dev"
    test-component:
      metadata:
        labels:
          version: "v1"
          experimental: "true"
      vars:
        test: "true"
