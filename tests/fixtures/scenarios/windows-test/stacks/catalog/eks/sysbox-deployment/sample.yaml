import:
  - catalog/eks/sysbox-deployment/defaults

components:
  terraform:
    eks/sysbox-deployment/sample:
      metadata:
        component: eks/sysbox-deployment
        inherits:
          - eks/sysbox-deployment/defaults
      vars:
        name: sysbox-sample
