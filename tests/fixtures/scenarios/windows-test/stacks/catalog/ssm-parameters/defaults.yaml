# SSM Parameters component defaults
components:
  terraform:
    ssm-parameters/defaults:
      metadata:
        type: abstract
        component: ssm-parameters
      vars:
        enabled: true
        # Empty params by default - each stack defines its own
        params: {}
