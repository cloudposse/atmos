import:
  - ../catalog/terraform/mock

components:
  terraform:
    mock:
      metadata:
        component: mock
        environment: dev
      backend:
        type: local
      vars:
        stage: dev
        environment: dev
        tenant: test
        foo: "dev-foo"
        bar: "dev-bar"
        baz: "dev-baz" 