import:
  - catalog/mock

components:
  terraform:
    mock:
      metadata:
        component: mock
        environment: prod
      backend:
        type: local
      vars:
        stage: prod
        environment: prod
        tenant: test
        foo: "prod-foo"
        bar: "prod-bar"
        baz: "prod-baz" 