# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: nonprod

components:
  terraform:
    component-1:
      metadata:
        component: mock
      settings:
        a: 1a
        b: 2
      vars:
        foo: "foo"
        bar: "bar"
        baz: "baz"

    component-2:
      metadata:
        component: mock
      vars:
        foo: "foo"
        bar: "bar"
        baz: "baz"
