# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: prod

components:
  terraform:
    component-1:
      metadata:
        component: mock
      settings:
        a: 4
        b: 5
        c: 6
      vars:
        foo: "{{ .settings.a }}"
        bar: "{{ .settings.b }}"
        baz: "{{ .settings.c }}"

    component-2:
      metadata:
        component: mock
      vars:
        foo: "foo"
        bar: "bar"
        baz: "baz"
