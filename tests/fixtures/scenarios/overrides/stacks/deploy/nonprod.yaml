# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: nonprod

import:
  - teams/team1
  - teams/team2

overrides: {}

terraform:
  overrides:
    vars:
      test:
        a: a-nonprod
        b: b-nonprod
        c: c-nonprod
