repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: redis
    namespace: redis
    chart: bitnami/redis
    version: {{ .Values.chart_version | quote }}
    values:
      - externalUrl: {{ requiredEnv "REDIS_URL" | quote }}
