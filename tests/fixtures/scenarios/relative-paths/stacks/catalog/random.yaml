# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

components:
  terraform:
    random:
      vars:
        stage: dev
        environment: ue2
        tenant: acme
        foo: "test-foo"
        bar: "test-bar"
        baz: "test-baz"
