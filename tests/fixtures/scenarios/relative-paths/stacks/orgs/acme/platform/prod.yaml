# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: prod

import:
  - ./_defaults
  - catalog/random

components:
  terraform:
    random:
      vars:
        stage: prod
        environment: ue2
        tenant: platform
        foo: "test-foo"
        bar: "test-bar"
        baz: "test-baz"
