{
  "base_path": "./",
  "components": {
    "terraform": {
      "base_path": "components/terraform"
    }
  },
  "stacks": {
    "base_path": "stacks",
    "included_paths": [
      "deploy/explicit-context"
    ],
    "name_template": "{{ .vars.stage }}"
  },
  "templates": {
    "settings": {
      "enabled": true,
      "sprig": {
        "enabled": true
      },
      "gomplate": {
        "enabled": true
      }
    }
  }
}
