– Preview Kubernetes manifest changes

```shell
 $ atmos kubernetes diff <component-name> -s <stack-name>
 $ atmos kubernetes diff --affected --base origin/main
 $ atmos kubernetes diff --all -s <stack-name>
```
