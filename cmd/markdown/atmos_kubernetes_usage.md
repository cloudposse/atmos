– Manage Kubernetes-native Atmos components

```shell
 $ atmos kubernetes render <component-name> -s <stack-name>
 $ atmos kubernetes diff <component-name> -s <stack-name>
 $ atmos kubernetes apply <component-name> -s <stack-name>
```
