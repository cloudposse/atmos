– Render Kubernetes manifests

```shell
 $ atmos kubernetes render <component-name> -s <stack-name>
 $ atmos kubernetes render <component-name> -s <stack-name> --output manifest.yaml
 $ atmos kubernetes render <component-name> -s <stack-name> --output-dir rendered --split
```
