– Apply Kubernetes manifests

```shell
 $ atmos kubernetes apply <component-name> -s <stack-name>
 $ atmos kubernetes apply --affected --base origin/main
 $ atmos kubernetes apply --all -s <stack-name>
```
