- Format the root vendor manifest and imported vendor manifests

```
 $ atmos vendor config format
```

- Format a specific vendor manifest tree

```
 $ atmos vendor config format --file ./vendor.yaml
```
