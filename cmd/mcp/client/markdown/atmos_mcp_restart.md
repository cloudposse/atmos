Stop and restart an external MCP server.

Useful for picking up configuration changes or recovering from server errors.

**Examples:**
```bash
# Restart a server by name.
atmos mcp restart aws-eks

# Example output:
# ✓ Restarted MCP server "aws-eks" (12 tools available)
```
