- Generate a Kubernetes token from an explicit Atmos GCP identity

```shell
atmos gcp gke token --identity example-deployer
```

- Generate a token using the inherited or sole configured identity

```shell
atmos gcp gke token
```
