hashicorp/terraform 1.15.8
opentofu/opentofu 1.12.6
peteretelej/tree v1.3.0
sharkdp/bat v0.26.1
hashicorp/packer 1.16.0
helm/helm v3.21.4
helmfile/helmfile v1.7.4
terraform-linters/tflint 0.64.0
# tofu is a required duplicate of opentofu/opentofu above, not stale: the
# atmos.ci "Format HCL" job runs `atmos toolchain exec -- tofu fmt ...`
# inside ghcr.io/cloudposse/atmos:1.215.0, whose toolchain resolver needs an
# exact `tofu` key in this file (unlike current HEAD, it doesn't auto-resolve
# bare short-name aliases against the registry). Keep this version equal to
# opentofu/opentofu's, above.
tofu 1.12.6
