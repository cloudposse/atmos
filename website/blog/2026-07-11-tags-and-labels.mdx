---
slug: tags-and-labels
title: "Tags and Labels: a First-Class Way to Categorize and Select Anything in Atmos"
authors: [osterman]
tags: [feature]
---

A component's name and its place in your stack hierarchy tell you how it's organized, not what it is. `vpc` in `stacks/orgs/acme/prod/network.yaml` doesn't say it's tier-1, or that platform owns it, or that it's in scope for SOX — and a real component is usually several of those things simultaneously, none of which fit into a single folder structure. Your AWS accounts and auth identities have the same problem: `prod-admin` doesn't tell you it's the production one, or the elevated one, until you already know that.

Tags and labels let you say that directly, so you can select and act on infrastructure by what it is — production, tier-1, platform-owned — instead of by what it happens to be called.

<!--truncate-->

## The Problem

Infrastructure gets organized once, by folder and by name, but you need to operate on it along dimensions that structure doesn't capture:

- **One hierarchy can't express every way you want to group things.** A component's path tells you its org, account, and region; it doesn't tell you it's tier-1, or SOX-scoped, or owned by platform — and it's usually several of those at once.
- **That meaning ends up living outside the system instead of on the resource.** A `--query` expression like `.settings.tier == "network"` encodes "tier-1" for one command; a hand-maintained `--components` list encodes it for another. Neither travels with the component, and both go stale the moment something changes.
- **The same gap shows up wherever Atmos manages many things.** Components across every type (Terraform, Kubernetes, Helm, containers), plus auth identities and providers, all needed a way to carry their own meaning instead of relying on someone remembering it.

## The Fix

Atmos now supports two kinds of metadata for categorization, matching how you'd actually query them:

- **Tags** — a simple list of words, for basic categorical membership. Filtering matches *any* of the given tags.
- **Labels** — a set of key-value pairs, for richer metadata. Filtering requires *all* of the given key=value pairs, the same semantics as a Kubernetes label selector.

```yaml title="stacks/orgs/acme/prod/network.yaml"
components:
  terraform:
    vpc:
      metadata:
        tags: [production, networking, tier-1]
        labels:
          cost-center: platform
          compliance: sox
```

```yaml title="atmos.yaml"
auth:
  providers:
    sso-prod:
      kind: aws/iam-identity-center
      start_url: https://my-org.awsapps.com/start
      tags: [production, aws, sso]

  identities:
    prod-admin:
      kind: aws/permission-set
      via:
        provider: sso-prod
      principal:
        name: AdministratorAccess
        account: {name: production}
      tags: [admin, production, elevated-access]
```

## How to Use It

**Listing and filtering components:**

```shell
atmos list components --tags production,tier-1
atmos list components --labels cost-center=platform,compliance=sox
```

**Bulk operations** — `--tags`/`--labels` compose with `--all`/`--affected` everywhere Atmos already supports bulk selection, across every component type:

```shell
atmos terraform apply --affected --tags production
atmos kubernetes apply --all --labels cost-center=platform
atmos helm apply --affected --tags production
atmos container up --all --tags production
```

Because this lives in the shared component registry (`pkg/component`), any component type — built-in or custom — that adopts the standard bulk-execution pattern gets `--tags`/`--labels` filtering automatically, with no per-type code.

**The same filtering works for auth**, since providers and identities are tagged the same way:

```shell
atmos auth list --tags production
atmos auth login --tags admin,production   # auto-selects if exactly one match, else prompts
atmos auth logout --tags production        # logs out every matching provider
```

**Bridging into your modules** — two new YAML functions read a component's own `metadata.tags`/`metadata.labels` without the usual `!template`/`toJson` boilerplate. The most common use is feeding `metadata.labels` into the `var.tags` a `terraform-null-label`-style module expects (Terraform and AWS call a map "tags"; Atmos calls it a "label" — same data, different name):

```yaml title="stacks/orgs/acme/prod/network.yaml"
components:
  terraform:
    vpc:
      metadata:
        labels:
          Namespace: eg
          Environment: prod
      vars:
        tags: !labels   # var.tags (map) <- metadata.labels
```

`!tags`, `!labels.keys`, and `!labels.values` round out the family for when a module wants the list form or just the map's keys or values.

## Get Involved

Tags and labels are supported today on components across terraform, Kubernetes, Helm, and container components, and on auth identities and providers. Auto-discovering tags/labels from cloud provider metadata (for example, mapping AWS SSO PermissionSet tags into Atmos labels automatically) is a natural next step — if that's something you need, open an issue and let us know your use case.
