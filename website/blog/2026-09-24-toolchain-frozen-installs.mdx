---
slug: toolchain-frozen-installs
title: "Consistent Toolchain Behavior Across Project Directories"
authors: [osterman]
tags: [feature, bugfix]
---

Working from a component directory should not mean setting up your tools again or finding new toolchain files scattered through your checkout. Your project's tool versions and settings should follow you as you move around it. Atmos [toolchain installations](/cli/configuration/toolchain) now resolve configured paths consistently from the project base, install missing tools without changing your version declarations, and keep standalone Atmos bootstrap files in XDG storage.

<!--truncate-->

## The Problem

Developers run commands from the repository root, component directories, and scripts. When toolchain paths depend on the current working directory, those entry points can look for different installations or write files in different places—even though they belong to the same project.

Automatic installation also made simply running a command look like a dependency change. Atmos version switching could leave project files in a directory that had no project at all. Together, these behaviors made users manage where commands ran and clean up files afterward.

## The Fix

Configured relative toolchain paths now resolve from the project's base path. Running from a subdirectory uses the same configured installation, version declarations, and artifact lockfile. Binaries continue to use shared XDG cache storage by default, so each working directory does not need its own installation.

When a command needs a missing tool, Atmos installs it without changing the versions your project declares. Atmos version switching follows the active project's toolchain settings too; outside a project, it keeps binaries and installation metadata in XDG storage.

For CI, opt-in frozen mode requires the artifacts to be recorded in advance and leaves the lockfile unchanged. Ordinary installs honor existing checksums and record missing version or platform entries after successful installation.

These changes use the existing files. The declaration file remains `.tool-versions`, and the artifact lockfile remains `toolchain.lock.yaml`. The `.tool-versions.lock` sidecar coordinates concurrent access; it is not another dependency declaration.

## How to Use It

Keep project declarations and the artifact lockfile under the configured project base while using the default shared binary storage:

```yaml title="atmos.yaml"
toolchain:
  versions_file: .tool-versions
  use_lock_file: true
  lock_file: toolchain.lock.yaml
```

With that configuration, those paths stay the same when you run Atmos from a project subdirectory. Tools are installed when a command needs them; changing directories alone does not trigger installation.

Populate the artifact lockfile with [toolchain install](/cli/commands/toolchain/install), then enable frozen mode in CI:

```shell
atmos toolchain install
ATMOS_TOOLCHAIN_FROZEN_LOCK_FILE=true atmos toolchain install
```

You can also enable frozen mode in configuration:

```yaml title="atmos.yaml"
toolchain:
  use_lock_file: true
  lock_file: toolchain.lock.yaml
  frozen_lock_file: true
```

Frozen mode requires a complete version and platform entry, including the artifact URL and checksum, even when the binary is already installed. It prohibits artifact lockfile writes and rejects lockfile refreshes. Prepare entries for every CI platform before enabling it.

**Migration:** Relative `install_path`, `versions_file`, and `lock_file` paths now resolve against the configured project base path. If you relied on paths relative to the invocation directory, set an absolute path or adjust your configuration. Automatic installs no longer add declarations; use intentional management commands to change them. These behavior fixes apply independently of the config edition pin; frozen mode defaults to disabled.

## Get Involved

Try running your usual commands from a project subdirectory and share feedback in [GitHub Discussions](https://github.com/cloudposse/atmos/discussions). See the [toolchain configuration reference](/cli/configuration/toolchain) for path and lockfile settings.
