---
title: atmos aws cloudformation backend delete
sidebar_label: delete
sidebar_class_name: command
id: delete
description: Permanently delete the S3 bucket used by the component's template-packaging backend.
---
import Intro from '@site/src/components/Intro'
import CastPlayer from '@site/src/components/CastPlayer'
import Experimental from '@site/src/components/Experimental'

<Intro>
  Permanently delete the S3 bucket referenced by the component's resolved
  `kind: aws/s3` provision target, and every object it contains (including all
  versions and delete markers). Requires `--force`; this action cannot be
  undone.
</Intro>

<Experimental />

<CastPlayer title="atmos aws cloudformation backend delete --help" src="/casts/screengrabs/atmos-aws-cloudformation-backend-delete--help.cast" static chrome />

## Usage

```shell
atmos aws cloudformation backend delete <component> --stack <stack> --force [options]
```

```shell
atmos aws cloudformation backend delete vpc -s plat-ue2-dev --force
```

Disambiguate when the component declares more than one `kind: aws/s3` target:

```shell
atmos aws cloudformation backend delete vpc -s plat-ue2-dev --target artifacts --force
```

## Flags

<dl>
  <dt>`--stack`, `-s` <em>(required)</em></dt>
  <dd>Atmos stack.</dd>

  <dt>`--force` <em>(required)</em></dt>
  <dd>Confirm permanent deletion. Without it, `backend delete` fails immediately without touching the bucket.</dd>

  <dt>`--target` <em>(optional)</em></dt>
  <dd>The `kind: aws/s3` provision target to use. Required when more than one is declared.</dd>

  <dt>`--identity`, `-i` <em>(optional)</em></dt>
  <dd>Atmos identity to authenticate with.</dd>
</dl>

## Behavior

`backend delete` lists every object and version in the bucket, warns how many
will be removed (including a separate count of `.tfstate`-suffixed keys, in
case the bucket is shared with an unrelated Terraform backend), deletes them
all in batches, and finally deletes the now-empty bucket.
