---
title: atmos toolchain list
sidebar_label: list
sidebar_class_name: command
id: list
---
import Screengrab from '@site/src/components/Screengrab'
import Terminal from '@site/src/components/Terminal'

:::note purpose
Use this command to list the cli tools added from the aqua repositories.
:::

<Screengrab title="atmos toolchain list --help" slug="atmos-toolchain-list--help" />

## Usage

Execute the `atmos toolchain list` command like this:

```shell
atmos toolchain list
```

## Examples

```shell
atmos toolchain list
```
