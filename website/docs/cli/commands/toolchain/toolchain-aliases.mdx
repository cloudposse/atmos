---
title: atmos toolchain aliases
sidebar_label: aliases
sidebar_class_name: command
id: aliases
---
import Screengrab from '@site/src/components/Screengrab'
import Terminal from '@site/src/components/Terminal'

:::note purpose
Use this command to get the aliases configured.
:::

<Screengrab title="atmos toolchain aliases --help" slug="atmos-toolchain-aliases--help" />

## Usage

Execute the `atmos toolchain aliases` command like this:

```shell
atmos toolchain aliases
```

## Examples

```shell
atmos toolchain aliases
```
