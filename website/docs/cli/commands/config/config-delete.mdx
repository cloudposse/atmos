---
title: atmos config delete
sidebar_label: delete
sidebar_class_name: command
id: config-delete
description: Delete a value from atmos.yaml by dot-notation path.
---
import Intro from '@site/src/components/Intro'

<Intro>
Delete a value from your `atmos.yaml` configuration using a dot-notation path,
preserving the rest of the file's comments, anchors, and formatting.
</Intro>

## Usage

```shell
atmos config delete <path>
```

The `del` and `unset` aliases are also accepted.

## Examples

```shell
atmos config delete components.terraform.append_user_agent
atmos config del logs.file
```

## Arguments

<dl>
  <dt>`<path>` (required)</dt>
  <dd>Dot-notation path to the value to delete.</dd>
</dl>

## Flags

<dl>
  <dt>`--config` (string, inherited)</dt>
  <dd>Target a specific `atmos.yaml` file instead of the one discovered in the current directory or git root.</dd>
</dl>
