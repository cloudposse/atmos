---
title: atmos terraform logout
sidebar_label: logout
sidebar_class_name: command
id: logout
---
import Screengrab from '@site/src/components/Screengrab'
import Terminal from '@site/src/components/Terminal'

:::note purpose
Use this command to remove stored API tokens for Terraform Cloud or Terraform Enterprise.
:::

<Screengrab title="atmos terraform logout --help" slug="atmos-terraform-logout--help" />

## Usage

Execute the `terraform logout` command like this:

```shell
atmos terraform logout <component> -s <stack> [options]
```

This command removes locally-stored API tokens for Terraform Cloud or Terraform Enterprise.

## Examples

### Logout from Terraform Cloud

```shell
# Logout from app.terraform.io (default)
atmos terraform logout vpc -s dev
```

### Logout from Terraform Enterprise

```shell
# Logout from a specific host
atmos terraform logout vpc -s dev terraform.company.com
```

## Arguments

<dl>
    <dt>`component` <em>(required)</em></dt>
    <dd>
        Atmos component name.
    </dd>
</dl>

## Flags

<dl>
    <dt>`--stack` / `-s` <em>(required)</em></dt>
    <dd>
        Atmos stack name where the component is defined.
    </dd>

    <dt>`--skip-init` <em>(optional)</em></dt>
    <dd>
        Skip running `terraform init` before executing the command.

        ```shell
        atmos terraform logout vpc -s dev --skip-init
        ```
    </dd>

    <dt>`--dry-run` <em>(optional)</em></dt>
    <dd>
        Show what would be executed without actually running the command.

        ```shell
        atmos terraform logout vpc -s dev --dry-run
        ```
    </dd>
</dl>

## Native Terraform Flags

This command supports native `terraform logout` flags such as Takes an optional hostname argument to specify which credentials to remove..

## Related Commands

- [`atmos terraform plan`](/cli/commands/terraform/terraform-plan) - Generate execution plan
- [`atmos terraform apply`](/cli/commands/terraform/terraform-apply) - Apply changes
- [`atmos terraform init`](/cli/commands/terraform/terraform-init) - Initialize working directory
