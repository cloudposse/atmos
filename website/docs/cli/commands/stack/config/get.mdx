---
title: atmos stack config get
sidebar_label: get
sidebar_class_name: command
id: get
description: Read a component-relative value from a stack and show which manifest defines it.
---
import Intro from '@site/src/components/Intro'
import Screengrab from '@site/src/components/Screengrab'

<Intro>
Read the effective value of a component-relative dot-path (e.g. `vars.region`) for a
component in a stack, and see which manifest file currently defines it. This is the
canonical form; [`atmos stack get`](/cli/commands/stack/stack-get) is a convenience
alias for this command.
</Intro>

<Screengrab title="atmos stack config get --help" slug="atmos-stack-config-get--help" />

## Usage

```shell
atmos stack config get <path> -s <stack> -c <component>
```

## Examples

```shell
atmos stack config get vars.region -s plat-ue2-prod -c vpc
```

## Arguments

<dl>
  <dt>`<path>` (required)</dt>
  <dd>Component-relative dot-notation path (e.g. `vars.region`).</dd>
</dl>

## Flags

<dl>
  <dt>`--stack` / `-s` (string, required)</dt>
  <dd>The stack name.</dd>
  <dt>`--component` / `-c` (string, required)</dt>
  <dd>The component name.</dd>
</dl>

## Related

- Alias: [`atmos stack get`](/cli/commands/stack/stack-get)
