---
title: atmos version track list
sidebar_position: 10
sidebar_label: list
sidebar_class_name: command
description: List configured version tracks.
---

List the configured version tracks from `atmos.yaml`.

## Usage

```shell
atmos version track list [flags]
```

## Examples

```shell
# Print track names as YAML
atmos version track list

# Print track names as JSON
atmos version track list --format=json
```
