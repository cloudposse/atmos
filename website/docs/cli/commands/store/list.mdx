---
title: atmos store list
sidebar_label: list
sidebar_class_name: command
id: list
description: List the store backends configured in atmos.yaml along with their kind and capabilities.
---
import CastPlayer from '@site/src/components/CastPlayer'
import Intro from '@site/src/components/Intro'

<Intro>
List the store backends configured under `stores:` in `atmos.yaml`. For each backend, this command shows its kind. It also shows whether the backend is a secret backend. It also shows which optional capabilities the backend supports, such as deletion or a local existence check. This command lists configured backends only. It does not list the keys stored inside them. Most backends do not support key enumeration.
</Intro>

<CastPlayer title="atmos store list --help" src="/casts/screengrabs/atmos-store-list--help.cast" static chrome />

## Usage

```shell
atmos store list [flags]
```

## Examples

```shell
# List all configured store backends
atmos store list

# Pipeline-friendly output
atmos store list --format=json
```

## Arguments

<dl>
  <dt>n/a</dt>
  <dd>No positional arguments.</dd>
</dl>

## Flags

<dl>
  <dt>`--format` <em>(alias `-f`)</em></dt>
  <dd>
    The output format. Choices are `table`, `json`, `yaml`, `csv`, or `tsv`. On a terminal, the default is a styled table. When piped, the default is plain, delimited output.
  </dd>
</dl>

## See Also

- [atmos store](/cli/commands/store/usage) — Overview of the store command group
- [Stores configuration](/cli/configuration/stores)
