# Platform Team Defaults
import:
  - orgs/acme/_defaults

vars:
  tenant: plat
  tags:
    Team: Platform

# Platform team uses OpenTofu for all terraform operations
terraform:
  dependencies:
    tools:
      opentofu: "1.10.0"
