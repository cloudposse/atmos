# Dev stack — keeps using existing .tfvars files via !include.
# Reference with: atmos terraform plan vpc -s dev
name: dev

import:
  - _defaults

vars:
  environment: dev

components:
  terraform:
    vpc:
      vars: !include ../legacy/dev.tfvars
