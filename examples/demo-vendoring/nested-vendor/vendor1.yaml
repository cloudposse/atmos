apiVersion: atmos/v1
kind: AtmosVendorConfig
metadata:
  name: vendor-test-1
  description: Test vendor config 1
spec:
  sources:
    - component: "ipinfo"
      source: "github.com/cloudposse/atmos.git//examples/demo-library/{{ .Component }}?ref={{.Version}}"
      version: "main"
      targets:
        - "components/terraform/{{ .Component }}/{{.Version}}"
      tags:
        - demo