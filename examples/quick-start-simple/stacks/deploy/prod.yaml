# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: prod

import:
  - catalog/mycomponent

components:
  terraform:
    mycomponent:
      vars:
        foo: "custom-foo-prod"
        baz: "custom-baz-prod"

