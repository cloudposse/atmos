# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

vars:
  stage: dev

import:
  - catalog/mycomponent

components:
  terraform:
    mycomponent:
      vars:
        foo: "custom-foo-dev"
        baz: "custom-baz-dev"
