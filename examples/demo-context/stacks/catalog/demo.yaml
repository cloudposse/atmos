components:
  terraform:
    demo:
      metadata:
        type: abstract
        component: petset
      providers:
        context:
          values:
            name: petset
      vars:
        pet: "dog"
