# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

components:
  terraform:
    demo:
      metadata:
        type: abstract
        component: petset
      providers:
        context:
          values:
            name: petset
      vars:
        pet: "dog"
