import:
  - ue2-globals
  - catalog/test-component
  - catalog/vpc

vars:
  stage: prod

terraform:
  vars: {}

helmfile:
  vars: {}

components:
  terraform:
    vpc:
      vars:
        cidr_block: 10.8.0.0/18
