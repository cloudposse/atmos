
# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

import:
- catalog/emulator/aws
- catalog/demo

vars:
  stage: dev

components:
  terraform:
    demo:
      metadata:
        type: real
      backend:
        s3:
          bucket: backend-provisioning-dev-tfstate
