# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

import:
  - _defaults

vars:
  stage: staging

components:
  terraform:
    foundation:
      vars:
        force_destroy: true
        secret_payload: staging-local-secret
