name: validation
description: Validate the landing zone stacks and components.

workflows:
  all:
    description: Validate all stacks, then the bucket component in every stage.
    steps:
      - command: validate stacks
      - command: validate component bucket -s dev
      - command: validate component bucket -s staging
      - command: validate component bucket -s prod
