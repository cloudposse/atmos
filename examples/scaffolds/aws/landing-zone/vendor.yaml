apiVersion: atmos/v1
kind: AtmosVendorConfig
metadata:
  name: landing-zone-vendor
  description: Component vendoring for the landing zone.
# This scaffold ships self-contained local components so it runs on the emulator
# with no network access. Add remote sources here to vendor real components, e.g.:
#
# spec:
#   sources:
#     - component: "vpc"
#       source: "github.com/cloudposse/terraform-aws-components.git//modules/vpc?ref={{.Version}}"
#       version: "1.372.0"
#       targets:
#         - "components/terraform/vpc"
spec:
  sources: []
