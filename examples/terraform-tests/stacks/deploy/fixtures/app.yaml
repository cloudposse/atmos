# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

import:
  - catalog/emulator/aws
  - catalog/app

vars:
  stage: fixtures

components:
  terraform:
    vpc:
      metadata:
        type: real
    app:
      metadata:
        type: real
      vars:
        environment: fixtures
      test:
        vars:
          expected_bucket_name: atmos-demo-test
          fixture_vpc_id: !terraform.state vpc fixtures vpc_id
