vars:
  namespace: acme

# Terraform S3 backend configuration
#terraform:
#  backend_type: s3
#  backend:
#    s3:
#      acl: "bucket-owner-full-control"
#      encrypt: true
#      bucket: "your-s3-bucket-name"
#      dynamodb_table: "your-dynamodb-table-name"
#      key: "terraform.tfstate"
#      region: "your-aws-region"
#      role_arn: "arn:aws:iam::<your account ID>:role/<IAM Role with permissions to access the Terraform backend>"

terraform:
  settings:
    test1: true
  vars:
    test: "{{ .settings.test1 }}"
    test1: "{{ if .settings.test1 }}true{{ else }}false{{ end }}"
    test2: '{{ printf "%t" .settings.test1 }}'
    test3: '{{ html .settings.test1 }}'
    test4: '{{ strings.Quote .settings.test1 }}'
    test5: '{{ .settings.test1 | strings.ReplaceAll "t" "" }}'
