watch_readme() {
  if [ -f "./README.md" ]; then
    echo "Contents of README.md:"
    cat "./README.md"
  fi
}

export DIRENV_WATCHES=$(find . -type d -exec echo "direnv allow {}" \;)
for watch in $DIRENV_WATCHES; do
  eval "$watch"
done

watch_readme
