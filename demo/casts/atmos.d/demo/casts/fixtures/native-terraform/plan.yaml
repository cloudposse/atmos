commands:
- name: casts
  commands:
  - name: generate
    commands:
    - name: demo
      commands:
      - name: casts
        commands:
        - name: fixtures
          commands:
          - name: native-terraform
            commands:
            - name: plan
              description: Regenerate the Terraform plan cast
              env:
                PATH: '{{ env "PWD" }}/../../.context/bin:{{ env "PATH" }}'
              steps:
              - type: shell
                name: setup
                command: atmos casts setup
              - type: shell
                name: clean
                command: rm -f ../../website/static/casts/demo/casts/fixtures/native-terraform/plan.cast
              - type: cast
                name: plan
                working_directory: "../../.context/cast-fixtures/native-terraform"
                command: atmos terraform plan vpc -s dev
                mode: steps
                width: 120
                height: 36
                output:
                  cast: "../../website/static/casts/demo/casts/fixtures/native-terraform/plan.cast"
                steps:
                - type: shell
                  name: plan
                  working_directory: "../../.context/cast-fixtures/native-terraform"
                  env:
                    TERM: xterm-256color
                    COLORTERM: truecolor
                    ATMOS_FORCE_COLOR: '1'
                    CLICOLOR_FORCE: '1'
                    FORCE_COLOR: '1'
                    ATMOS_FORCE_TTY: 'true'
                    ATMOS_KEYRING_TYPE: memory
                  command: atmos terraform plan vpc -s dev
              - type: shell
                name: validate
                command: atmos casts validate demo casts fixtures native-terraform
                  plan
  - name: validate
    commands:
    - name: demo
      commands:
      - name: casts
        commands:
        - name: fixtures
          commands:
          - name: native-terraform
            commands:
            - name: plan
              description: Validate the Terraform plan cast
              steps:
              - type: script
                name: validate
                interpreter: python3
                script: |
                  from pathlib import Path
                  text = Path("../../website/static/casts/demo/casts/fixtures/native-terraform/plan.cast").read_text()
                  for needle in ["Terraform will perform the following actions", "Plan:", "vpc_cidr"]:
                      if needle not in text:
                          raise SystemExit(f"terraform plan cast missing {needle!r}")
                  for bad in ["# Error", "Incorrect Usage", "unknown flag", "panic", "not found"]:
                      if bad in text:
                          raise SystemExit(f"terraform plan cast contains {bad!r}")
