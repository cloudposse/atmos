commands:
  - name: casts generate examples terraform-tests stack-discovery
    description: Regenerate the Terraform Tests example cast
    env: !include cast-defaults.yaml .env.command
    steps:
      - type: shell
        name: setup
        command: atmos casts setup
      - type: cast
        name: stack-discovery
        working_directory: "../../.context/cast-fixtures/terraform-tests"
        title: "Terraform Tests: discover emulator-backed fixtures"
        mode: steps
        defaults:
          cast: !include cast-defaults.yaml .cast
          simulate: !include cast-defaults.yaml .simulate
        env: !include cast-defaults.yaml .env.recording
        show:
          labels: false
        output:
          mode: raw
          cast: "../../website/static/casts/examples/terraform-tests/stack-discovery.cast"
        steps:
          - type: simulate
            text: "# validate the test stack manifests"
          - type: simulate
            text: &validate_cmd atmos validate stacks
          - type: shell
            name: validate-stacks
            duration: 450ms
            interval: 750ms
            command: *validate_cmd
          - type: simulate
            text: "# discover the emulator, fixture, and app components"
          - type: simulate
            text: &components_cmd atmos list components
          - type: shell
            name: list-components
            duration: 450ms
            interval: 750ms
            command: *components_cmd
      - type: shell
        name: validate
        command: atmos casts validate examples terraform-tests stack-discovery
  - name: casts validate examples terraform-tests stack-discovery
    description: Validate the Terraform Tests example cast
    steps:
      - type: script
        name: validate
        interpreter: python3
        script: |
          import os, sys
          sys.path.insert(0, os.getcwd())
          from cast_checks import load_text, strip_ansi, assert_no_experimental, assert_no_error_output, assert_no_local_paths
          text = load_text("../../website/static/casts/examples/terraform-tests/stack-discovery.cast")
          plain = strip_ansi(text)
          assert_no_experimental(text)
          for needle in ["atmos validate stacks", "All stacks validated successfully", "atmos list components", "app", "aws", "vpc"]:
              if needle not in plain:
                  raise SystemExit(f"terraform-tests cast missing {needle!r}")
          assert_no_error_output(text)
          assert_no_local_paths(text)
