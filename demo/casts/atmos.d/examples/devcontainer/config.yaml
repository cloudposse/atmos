commands:
  - name: casts generate examples devcontainer config
    description: Regenerate the Devcontainer example cast
    env: !include cast-defaults.yaml .env.command
    steps:
      - type: shell
        name: setup
        command: atmos casts setup
      - type: cast
        name: config
        working_directory: "../../.context/cast-fixtures/devcontainer"
        title: "Devcontainer: discover and inspect shells"
        mode: session
        shell: /bin/sh
        write_rate: 25ms
        key_interval: 250ms
        timeout: 30s
        defaults:
          cast: !include cast-defaults.yaml .cast
        env:
          TERM: dumb
          NO_COLOR: '1'
          ATMOS_FORCE_TTY: 'true'
          ATMOS_KEYRING_TYPE: memory
          ATMOS_EXPERIMENTAL: silence
          ATMOS_LOGS_LEVEL: Warning
          PS1: '$ '
        show:
          labels: false
        output:
          mode: raw
          cast: "../../website/static/casts/examples/devcontainer/config.cast"
        steps:
          - type: pause
            duration: 500ms
          - type: write
            text: "atmos devcontainer list"
          - type: key
            key: enter
          - type: wait
            text: "geodesic"
            timeout: 10s
          - type: pause
            duration: 500ms
          - type: write
            text: "atmos devcontainer config geodesic"
          - type: key
            key: enter
          - type: wait
            text: "Workspace Folder: /workspace"
            timeout: 10s
      - type: shell
        name: validate
        command: atmos casts validate examples devcontainer config
  - name: casts validate examples devcontainer config
    description: Validate the Devcontainer example cast
    steps:
      - type: script
        name: validate
        interpreter: python3
        script: |
          import os, sys
          sys.path.insert(0, os.getcwd())
          from cast_checks import load_text, strip_ansi, assert_no_experimental, assert_no_error_output, assert_no_local_paths
          text = load_text("../../website/static/casts/examples/devcontainer/config.cast")
          plain = strip_ansi(text)
          assert_no_experimental(text)
          for needle in [
              "atmos devcontainer list",
              "geodesic",
              "terraform",
              "python",
              "atmos devcontainer config geodesic",
              "Runtime: podman",
              "cloudposse/geodesic:latest",
              "Workspace Folder: /workspace",
          ]:
              if needle not in plain:
                  raise SystemExit(f"devcontainer cast missing {needle!r}")
          assert_no_error_output(text)
          assert_no_local_paths(text)
