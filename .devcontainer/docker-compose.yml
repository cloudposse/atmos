include:
  - path: "../examples/demo-localstack/docker-compose.yml"

services:
  devcontainer:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces:cached
    links:
      - localstack
    command: sleep infinity
