{
    "scripts": {
        "setup": "cd website && source ~/.nvm/nvm.sh && nvm install && pnpm install --shamefully-hoist",
        "run": "cd website && source ~/.nvm/nvm.sh && nvm install && pnpm install --shamefully-hoist && pnpm exec docusaurus start --port $CONDUCTOR_PORT"
    }
}
